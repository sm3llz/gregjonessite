{
  "name": "gregjonessite",
  "version": "1.0.0",
  "description": "Personal command interface for Greg Jones — Family Office, Venture Capital, Automation.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "lint": "astro check",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "11",
    "astro": "^4.16.18"
  },
  "engines": {
    "node": ">=18.20.0"
  },
  "devDependencies": {
    "wrangler": "^4.99.0"
  }
}
